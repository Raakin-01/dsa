reversing of array:
#include<stdio.h>

int main(){
    int n;
    scanf("%d",&n);

    int a[n];
    for(int i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    for(int j=n-1;j>=0;j--){
        printf("%d ",a[j]);
    }
    return 0;
}
-----------------------------------------------
alternate elements:
#include<stdio.h>
int main(){
    int arr[] = {10, 20, 30, 40, 50};
    int n=sizeof(arr)/sizeof(arr[0]);
    for(int i=0;i<n;i=i+2){
        printf("%d ",arr[i]);
    }
}
